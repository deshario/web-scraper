{
  "name": "scrapper",
  "description": "Data scrapper",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "dev": "npx lerna run dev --parallel"
  },
  "workspaces": {
    "packages": [
      "apps/**"
    ]
  }
}
